/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;

module.exports = {
  reactStrictMode: true,
  experimental: {
    emotion: true,
  },
};

module.exports = {
  images: {
    domains: ['res.cloudinary.com'],
  },
};
